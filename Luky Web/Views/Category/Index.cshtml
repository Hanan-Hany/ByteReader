@model IEnumerable<Category>

   <div class="container">
       <div class="row pt-4 pb-3">
           <div class="col-6">
               <h2 class="text-primary">
                   Category List
               </h2>
           </div>
            <div class="col-6 text-end ">
            <a asp-controller="Category"  asp-action="Create" class="btn btn-primary"> 
                <i class="bi bi-plus-circle-dotted"></i> Create New Category
            </a>
           </div>
       </div>
   <table class="table table-bordered table-striped ">
       <thead>
           <tr>
               <th>
                    @Html.DisplayNameFor(Category => Category.Name)
               </th>
               <th>
                   @Html.DisplayNameFor(Category=> Category.DisplayOrder)
               </th>
           </tr>
       </thead>
      
       <tbody>
            @if(Model.Count() > 0)  {
            @foreach (var category in Model)
            {<tr>
                <td>
                    @Html.DisplayFor(Category => category.Name)
                </td>
                <td>
                        @Html.DisplayFor(Category => category.DisplayOrder)
                </td>
            </tr>
                
            }
            }
        </tbody>
   </table>
</div>
 

